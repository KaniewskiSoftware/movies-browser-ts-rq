/**
 * This triple-slash directive instructs the TypeScript compiler to include the types for React scripts in the project.
 * 
 * @reference types - The types that the directive references
 * @reference types="react-scripts" - The specific types that the directive references
 */
/// <reference types="react-scripts" />
